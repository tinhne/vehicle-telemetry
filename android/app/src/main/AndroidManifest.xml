<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <!--
        android.hardware.type.automotive:
        Makes this app appear in Android Automotive OS launcher.
        required="false" = also installable on phones for dev.
    -->
    <uses-feature android:name="android.hardware.type.automotive" android:required="false"/>
    <uses-feature
        android:name="android.software.leanback"
        android:required="false" />
    <uses-feature
        android:name="android.hardware.touchscreen"
        android:required="false" />

    <application
        android:name=".VehicleTelemetryApp"
        android:banner="@drawable/ic_car"
        android:label="Vehicle Telemetry"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:theme="@style/Theme.VehicleTelemetry"
        android:allowBackup="false"
        android:networkSecurityConfig="@xml/network_security_config">

        <activity
            android:name=".presentation.main.MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:screenOrientation="landscape">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
                <!-- AAOS launcher category -->
                <category android:name="android.intent.category.LEANBACK_LAUNCHER"/>
            </intent-filter>
        </activity>

        <!--
            foregroundServiceType="dataSync":
            Required on Android 14+ for services maintaining
            persistent network connections (our WebSocket telemetry stream).
            connectedDevice was used previously but requires extra Bluetooth/USB/NFC
            permissions that don't apply to a pure network WebSocket connection.
        -->
        <service
            android:name=".service.TelemetryForegroundService"
            android:exported="false"
            android:foregroundServiceType="dataSync"/>
    </application>
</manifest>
